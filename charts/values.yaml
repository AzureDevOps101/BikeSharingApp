# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

bikesharingweb:
  ingress:
    annotations:
      kubernetes.io/ingress.class: traefik-azds  # Dev Spaces-specific
      buildID: ""
    hosts:
      - dev.bikesharingweb.5lrxwsdstk.sea.azds.io  # Assumes deployment to the 'dev' space

gateway:
  ingress:
    annotations:
      kubernetes.io/ingress.class: traefik-azds  # Dev Spaces-specific
      buildID: ""
    hosts:
      - dev.gateway.5lrxwsdstk.sea.azds.io  # Assumes deployment to the 'dev' space